services:
  auth-service:
    build:
      context: .
      dockerfile: Dockerfile
    image: tankistpro512/voice-chat-auth-service:latest
    container_name: voice-chat-auth-service
    # env_file:
    #   - .env.production.local
    networks:
      - voice-chat-network
    restart: unless-stopped

networks:
  voice-chat-network:
    external: true
